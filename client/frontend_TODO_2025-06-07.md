# ✅ TODO для фронту / Frontend TODO (v2025-06-13)

## 🔐 Аутентифікація / Authentication

- [x] Login + 2FA реалізовано
- [x] Google Login інтегровано
- [x] Role-based routing (`AdminRoute`, `PrivateRoute`)
- [x] Валідація форм:
  - [x] `ResetPasswordForm` — Yup
  - [x] `RegisterForm` — Yup
  - [x] `BookForm` (для `AddBook` і `EditBook`) — Yup
  - [x] `Newsletter.jsx` — Yup реалізована, але без i18n

## 🌍 Мультимовність / Internationalization

- [x] Перекладено: LoginForm, Header, LanguageSwitcher
- [~] Частково:
  - Navbar — ❌ всі назви лінків
  - Footer — ❌ 1 лінк
  - CartPage — ❌ повідомлення помилки
  - FavoritesPage — ❌ повідомлення помилки
  - AdminDashboard — ✅ без захардкоженого тексту
- [x] Newsletter — валідація не перекладена
- [x] ConfirmModal — повністю перекладено
- [~] Валідаційні повідомлення — частково через `t()`
- [~] Notifications — використовує `t()`, але не всі повідомлення мають ключі

## 📦 Redux Toolkit

- [x] Усі слайси реалізовано
- [x] Усі санки мають `try/catch` і обробку помилок
- [x] Додати глобальний error boundary для UI (опціонально)

## ✍️ Editable Pages

- [x] `EditablePage`, `EditableToolbar`, `ConfirmModal`, `ImageInsertModal`
- [x] Збереження чернеток (`/pages/:slug/draft`)
- [x] Автозбереження (через debounce) — ❌ debounce є, логіка не підключена
- [x] Переклад усіх модалок (Insert, Confirm, Table, Link) — всі модалки містять `t()`
- [x] Валідація HTML перед вставкою — реалізовано через ConfirmModal

## 💳 Оплата / Payment Integration

- [x] `SuccessPage`, `CancelPage`, `paymentService.js`
- [ ] Додати loading/spinner під час транзакцій
- [ ] Очищення кошика після оплати (`clearCart()`)

## 🧪 Тестування / Testing

- [ ] Інтегрувати Vitest або RTL
- [ ] Написати базові тести для Redux (auth, book, page)
- [ ] Snapshot тести для `BookList`, `EditablePage`

## 📱 UX/UI покращення

- [x] Responsive: модалки, сторінки, хедер
- [x] Реалізовано Burger Menu (`Navbar.jsx`)
- [ ] Додати alt до зображень
- [ ] SEO: додати `react-helmet`
- [ ] Виправити hover у Navbar, LanguageSwitcher
- [ ] Checkout UX (спінер, редирект, помилки)

## 🧩 Інше / Misc

- [x] Сторінка обраного (`FavoritesPage`)
- [x] User Management (`UserManagement.jsx`)
- [x] 404 fallback сторінка
- [x] Lazy Loading для `BookDetails`, `Orders`
- [x] Валідація контенту `EditablePage` перед збереженням — реалізовано через hasVisibleText()
